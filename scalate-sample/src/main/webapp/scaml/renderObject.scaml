%html
  %body
    %h2 View Objects
    %p This page renders a model object using its default template
    - import org.fusesource.scalate.sample.Person
    - val people = List(Person("James", "Strachan"), Person("Hiram", "Chirino"))
    - for (person <- people ) 
      - render(person)
